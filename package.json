{
  "name": "thote",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:lang": "pnpm -F=tree-sitter-typlet prebuildify && pnpm -F=tree-sitter-typlet build && pnpm -F=tree-sitter-typlet test && pnpm -F=typlet build",
    "test:lang": "pnpm -F=tree-sitter-typlet build && pnpm -F=tree-sitter-typlet test",
    "build": "pnpm -F=seeshat build"
  },
  "devDependencies": {
    "dprint": "^0.45.0"
  }
}
